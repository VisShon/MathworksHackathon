query GetInterviewer($where: InterviewerWhere) {
  interviewers(where: $where) {
    interviewList {
      candidate {
        name
        college
        track
        degree
        candidateId
      }
      timeEnd
      timeStart
      interviewStatus
    }
    email
    role
    skillset
    userName
  }
}